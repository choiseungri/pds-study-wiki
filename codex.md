# Codex 작업 기록

이 파일은 PDS 학습 위키를 만들면서 나온 아이디어, 결정, 시행착오를 계속 기록하는 곳이다.

## 현재 방향

- 기존 강의 HTML은 그대로 두고, 루트의 `index.html`에서 시간표형 홈 화면을 제공한다.
- 홈 목록 데이터는 `scripts/generate-site-data.mjs`가 HTML 파일을 읽어 `site-data.js`로 생성한다.
- 새 자료를 추가할 때는 `npm run build`를 실행해 홈 목록을 갱신한다.
- 무료 호스팅은 GitHub Pages + GitHub Actions를 1순위로 둔다. `main` 브랜치에 push하면 `.github/workflows/deploy.yml`이 자동 배포한다.

## 파일명 규칙

- 배포 안정성을 위해 강의 HTML 파일명은 ASCII로 고정한다.
- 형식은 `MMDD_교시_english-slug.html`이다.
- 예: `0511_2_public-health-policy.html`
- slug에는 영문 소문자, 숫자, 하이픈만 쓴다.
- 화면에 표시되는 한국어 제목은 파일명이 아니라 각 HTML 내부의 `<h1>` 또는 `<title>`에서 가져온다.

## 시행착오

- 처음에는 기존 한국어 파일명을 그대로 링크하는 방식으로 구성했다.
- 사용자가 배포 시 파일명 인코딩 문제가 생길 수 있다고 지적했다.
- 이에 따라 기존 23개 HTML을 영어 slug 파일명으로 rename했고, 빌드 스크립트에서 비 ASCII 또는 규칙 밖 파일명을 오류로 막도록 수정했다.
- 첫 빌드에서 `weekdayLabels` 상수를 사용한 뒤 선언하는 순서 오류가 발생했다. 상수를 상단으로 이동해 Node.js ESM 초기화 오류를 해결했다.
- `site-data.js`를 빌드와 동시에 병렬로 읽었더니 파일 생성 전에 읽기 명령이 먼저 실행되어 실패했다. 생성 산출물 확인은 빌드 후 순차 실행해야 한다.
- PowerShell `Get-Content`에서는 UTF-8 한글이 깨져 보일 수 있었다. 실제 파일 유효성은 `node --check site-data.js`와 브라우저 렌더링으로 확인했다.
- 브라우저 검증에서 같은 강의 링크가 시간표와 전체 목록에 각각 있어 `href`만으로는 locator가 2개 잡혔다. 검증 시에는 `.schedule-view` 또는 `.lecture-list`로 범위를 좁혀 확인했다.

## 다음 아이디어

- 실제 수업 시작/종료 시간이 확정되면 `site.config.json`의 `periods.time`을 채워 시간표 셀에 표시한다.
- GitHub 저장소가 연결되면 Codex가 `npm run build` 후 commit/push까지 수행해 재배포를 자동화할 수 있다.

## 2026-05-11 디자인 수정

- 사용자가 기존 시간표 디자인이 부족하다고 피드백했다.
- 시간표를 단순 HTML 표처럼 보이지 않게 주차 헤더, 고정 교시 축, 날짜 헤더, 태그별 색상 강의 블록으로 재구성했다.
- 빈 시간표 칸은 패턴 배경과 짧은 선으로 낮춰 보이게 하고, 실제 강의 카드에는 좌측 색상 바와 hover 상태를 추가했다.
- 전체 자료 카드도 시간표와 같은 태그 색상 체계를 공유하게 바꿨다.
- 사용자가 태그를 과목별이 아니라 담당 교수님별로 바꾸라고 요청했다.
- `site.config.json`에 `pageProfessors` 명시 매핑을 추가했다. 교수명은 박훈기, 한승훈, 김대호, 손정식, 신영전, 김민주, 이어진, 송은섭, 김원규로 정리했다.
- 생성 스크립트는 파일별 명시 매핑을 우선 사용하고, 새 파일에서 매핑이 빠졌을 때만 상단 메타 텍스트의 `professorRules`로 보조 추론한다.

## 2026-05-11 시간표 이미지 반영

- 사용자가 배치표 이미지를 제공했고, 괄호 속 이름이 담당 교수님이라고 정정했다.
- 현재 보유한 HTML 23개는 배치표의 괄호 속 이름 기준으로 `pageProfessors`를 유지한다.
- 2교시짜리 강의는 파일명 시작 교시를 유지하고 `site.config.json`의 `pageSpans`에서 칸 수를 지정하기로 했다.
- 우선 `0429_5`, `0430_1`, `0430_3`, `0430_5`, `0501_3`, `0506_1`, `0507_5` 강의를 2칸으로 표시한다.
- 홈 화면 렌더러는 span으로 덮인 다음 교시 칸을 만들지 않고, 시작 교시 셀이 CSS grid row span으로 두 칸을 차지하게 바꿨다.
- 사용자가 시간표를 까먹지 않게 저장해두라고 요청했다.
- 원본 이미지의 배치표 내용을 `timetable.md`에 전사했다. 앞으로 자료가 추가되면 이 파일을 기준으로 파일명, 담당 교수님, span을 맞춘다.

## 2026-05-11 배포 시행착오

- GitHub 저장소 `choiseungri/pds-study-wiki`를 공개 저장소로 만들고 `main` 브랜치에 첫 push를 했다.
- 첫 GitHub Actions 배포는 새 저장소에서 Pages가 아직 활성화되지 않아 `actions/configure-pages`가 Pages site를 찾지 못하며 실패했다.
- `.github/workflows/deploy.yml`의 `Configure Pages` 단계에 `enablement: true`를 추가해 첫 배포 시 Pages를 활성화하도록 수정했다.
- 두 번째 실행은 Actions 토큰이 Pages site 생성 권한을 얻지 못해 실패했다.
- GitHub 저장소 Settings > Pages에서 Source를 `GitHub Actions`로 직접 선택해 Pages를 활성화했다.
- 세 번째 실행은 성공했다.
- 배포 URL은 `https://choiseungri.github.io/pds-study-wiki/`이다.
- 배포 후 홈 화면에서 `PDS 학습 위키`, 시간표, 교수님 태그, `5-6교시 13:00-14:50` span 표기가 보이는지 확인했다.
- 개별 강의 URL `https://choiseungri.github.io/pds-study-wiki/0507_5_injury-death.html`도 열어 손상사 학습자료가 로딩되는 것을 확인했다.
## 2026-05-14 2025 총괄평가 퀴즈 HTML 초안

- 사용자가 25 총괄 시험을 웹에서 바로 푸는 퀴즈형 HTML로 먼저 만들어 보고 피드백을 주겠다고 요청했다.
- 이어서 위키 등록은 아직 하지 말고 HTML 파일만 만들라고 정정했다. 루트에 두면 `scripts/generate-site-data.mjs`가 자동 수집하거나 파일명 규칙 오류를 낼 수 있으므로 원본 PDF가 있는 `Y/2025-pds4-summative-quiz.html`에 독립 파일로 만들기로 했다.
- 사용자가 문항이 어느 강의에서 나왔는지 바로 확인해야 범위별로 풀 수 있다고 피드백했다. 이에 PDF의 대괄호 섹션명을 각 문항의 `source`로 유지하고, 강의별 필터/출처 배지/검색을 넣었다.
- `2025학년도 환자.의사.사회4 총괄평가 (251231).pdf`는 문제와 강의별 문항 수는 들어 있지만 별도 정답표가 없었다. 대부분의 정답은 문항 내용과 강의 주제 기준으로 입력했고, PDF에 `답 논란 ④⑤`라고 적힌 국민건강보험법 문항은 ④와 ⑤를 모두 정답 처리했다.
- 자연의학 멀티미디어 문항은 PDF 추출 텍스트에 실제 사진/영상이 없어 정답 검토가 필요하다. 현재는 니시의학으로 임시 입력하고 화면에 `검토 필요` 배지를 표시했다.
- Larry Dossey의 Distant healing 문항도 보기 중 정확한 개념 대응이 애매해 `답 없음`으로 임시 입력하고 `검토 필요` 배지를 표시했다.

## 2026-05-14 2025 총괄평가 퀴즈 1차 피드백

- 사용자가 디자인이 직관적이지만 너무 투박하다고 피드백했다. 헤더/통계/툴바/문항 카드의 시각 위계를 정리하고, 강의 필터를 좌측 고정 패널처럼 보이게 바꿨다.
- 사용자가 선지 종류는 동일하되 순서는 매번 섞이도록 요청했다. 각 보기 객체에 원래 번호와 정답 여부를 저장한 뒤, 페이지 로드 시 문항별 `shuffledOptions`를 생성하는 방식으로 수정했다.
- 정답 판정은 화면 번호가 아니라 원래 보기 번호 기준으로 저장한다. 따라서 보기 순서가 바뀌어도 정답/오답 판정과 정답 표시가 깨지지 않는다.
- 브라우저에서 로컬 페이지를 새로고침해 확인했다. 전체 24문항 표시, 자연의학 필터 4문항 표시, 보기 클릭 후 피드백 표시, 새로고침 후 Q1 보기 순서 변경을 확인했다.

## 2026-05-14 Y 폴더 시험지 일괄 퀴즈화

- 사용자가 `2025-pds4-summative-quiz.html`을 예시 틀로 삼아 2025 형성부터 역순으로 모든 시험지 HTML을 제작해 달라고 요청했다.
- `scripts/generate-y-quizzes.py`를 추가해 PDF/DOCX/HWP에서 문항을 추출하고, 같은 퀴즈 UI/선지 셔플/강의 필터 구조로 HTML을 생성하도록 했다.
- 최신 답지 PDF들은 정답 텍스트가 아니라 밑줄 또는 노란 형광 표시로 정답을 표시한다. PyMuPDF의 text line bbox와 drawing rect가 겹치는지 계산해 정답 선지를 자동 추출했다.
- 2025 형성 PDF는 `n+n문항` 구조라 형성 범위와 총괄 출제 문항이 함께 들어 있다. 최종 생성 결과는 197문항이며, 원본 표시 미확인 임시정답 2건과 구조 검토표시 6건을 남겼다.
- 2023 형성/총괄, 2022 형성/총괄, 2021 형성/총괄, 2020 형성/총괄은 대체로 정답 표시를 자동 인식했다. 일부 복원 누락/서술형/도형 문제는 `검토 필요`로 표시된다.
- 2019, 2018, 2017, 2016, 2015 자료는 서식이 일정하지 않다. 번호와 보기 분리가 심한 파일은 느슨한 번호 기반 파서를 적용했고, 정답 표시가 없거나 스캔 답지인 경우는 가능한 문제 카드만 만들었다.
- 생성된 파일은 `Y/2015-pds4-restored-quiz.html`부터 `Y/2025-pds4-formative-quiz.html`까지이며, 기존 `Y/2025-pds4-summative-quiz.html`은 유지했다.
- 브라우저에서 `2025-pds4-formative-quiz.html`, `2023-pds4-formative-quiz.html`, `2017-pds4-restored-quiz.html`, `2016-pds4-formative-quiz.html` 로드를 확인했다. `2023-pds4-formative-quiz.html`에서는 보기 클릭 후 피드백 표시와 새로고침 후 선지 순서 변경을 확인했다.

## 2026-05-14 퀴즈 검토 보정

- 사용자가 모든 HTML을 만든 뒤 문항 파싱 오류와 정답 동일성을 반드시 검토해야 한다고 지적했다.
- 2023 형성 Q2에서 PDF 줄 단위 추출이 그대로 남아 `보건의료기본법 / 시행령 / 상 ...`처럼 보이는 문제가 확인됐다. `normalize_stem()`에서 문항 본문 줄들을 공백으로 합치도록 바꾸고 전체 HTML을 재생성했다.
- 숫자 선지 앞의 `⚫` 마커가 보기로 들어가던 문제가 있었다. 숫자만 있는 보기 줄이 `is_noise()`로 먼저 제거되어 생긴 문제라, bullet pending 처리를 noise 제거보다 먼저 수행하도록 수정했다.
- `6.문보건복지부령...`처럼 PDF 추출에서 붙은 불필요한 선행 `문`을 정리하는 `cleanup_question_start()`를 추가했다.
- `선지 1개 + 정답 없음`처럼 퀴즈로 의미 없는 카드는 생성에서 제외하도록 했다. 단, 주관식처럼 정답이 있는 단일 보기 문항은 유지한다.
- `scripts/validate-y-quizzes.py`를 추가하고 `Y/quiz-validation-report.md`를 생성했다. 이 리포트는 파일별 문항 수, 자동 정답 확인 수, 미확인 정답 수, 구조 플래그를 보여준다.
- 브라우저에서 `2023-pds4-formative-quiz.html`을 다시 열어 Q2 문장이 한 줄로 합쳐진 것과 Q10의 불필요한 `문` 접두어가 사라진 것을 확인했다.

## 2026-05-14 퀴즈 전수 검수 2차 보정

- 사용자가 성폭력 상담 문항처럼 선지의 후행 줄이 새 문항으로 쪼개지는 오류가 많다고 지적했다.
- PDF 줄 객체에 `x0`, `y0` 좌표를 저장하고, 보기 후행 줄인지 새 문항인지 판단할 때 들여쓰기와 열 전환을 함께 보도록 수정했다. 이로써 `만지게 했니?`, `손가락을 넣었니?` 같은 후행 문장이 독립 문항으로 분리되는 문제를 제거했다.
- 원문에 `답 논란`, `정답 없음`, `문항 삭제`가 표시된 경우 정답을 빈 값으로 두지 않고 `검토 필요`로 표시하며, 명시된 후보나 임시 후보를 넣도록 바꾸었다. 삭제/정답 없음 문항은 채점 제외 성격의 검토 문항으로 남긴다.
- `※`로 시작하는 해설/이의제기 메모가 선지에 길게 붙어 다음 문항을 오염시키는 문제를 줄였다. 화면 표시용 문항/선지에서는 메모 본문을 제거하되, 정답 논란 및 삭제 정보는 `note`, `uncertain`, `deleted` 값으로 보존한다.
- 한 줄짜리 단답/주관식 문항은 퀴즈 카드로 유지하되 구조 리포트에서는 따로 보이게 했다. 이는 파싱 실패가 아니라 원문이 주관식 또는 답안형 복원 문항인 경우가 많기 때문이다.
- `2025-pds4-formative-quiz.html`의 감전사 삭제 문항, 연소혈종 단답 문항, 성과 관련된 사망 섹션 전환을 원문 흐름에 맞게 분리했다.
- 브라우저에서 `2023-pds4-formative-quiz.html`을 새로고침해 보건의료기본법 문항이 줄바꿈 없이 표시되는지 확인했다.
- `scripts/validate-y-quizzes.py`가 초기 수동 제작본인 `2025-pds4-summative-quiz.html`의 `const questions` 형식도 읽도록 확장했다. 검수 리포트에 2025 총괄 24문항도 포함된다.

## 2026-05-14 기출문항 위키 연결

- 사용자가 모든 시험지를 위키에 올리고 시간표 위에서 연도별 기출문항으로 들어갈 수 있게 해 달라고 요청했다.
- `index.html`의 시간표 섹션 바로 위에 `연도별 기출문항` 영역을 추가했다.
- `assets/home.js`에 `Y/` 폴더의 18개 퀴즈 HTML 링크를 2025년부터 2015년까지 역순으로 묶었다. 각 링크에는 시험 종류, 문항 수, 임시정답/검토표시 정보를 함께 보이게 했다.
- `assets/site.css`에는 연도별 행과 시험지 링크 스타일을 추가했다. 시간표보다 먼저 보이되 기존 위키 색상 체계와 반응형 레이아웃을 유지한다.
- `npm run build`는 루트의 미등록 파일 `0427_2_수정버전.html`이 파일명 규칙에 맞지 않아 실패했다. 이번 기출 섹션은 `site-data.js` 재생성이 필요 없는 정적 홈 렌더링으로 넣었다.
- 원본 PDF/DOCX/HWP는 커밋하지 않고 퀴즈 HTML 18개, 검수 리포트, 생성/검수 스크립트, 홈 변경만 `main`에 push했다.
- GitHub Pages 공개 URL에서 홈의 `연도별 기출문항` 마크업과 원격 `assets/home.js`의 2015/2025 퀴즈 링크, `Y/2025-pds4-formative-quiz.html` 로딩을 확인했다.

## 2026-05-14 4개 시험지 재검수 및 강의록 추가

- 사용자가 2018 형성, 2017 복원, 2016 복원, 2015 복원 시험지에 오류가 많다고 지적했다.
- 2018 형성은 깨진 PDF 텍스트 대신 `2018 PDS4 형성평가 복원.hwp`를 우선 파싱하도록 바꾸어 88문항으로 재제작했다.
- 2017/2015 복원 PDF는 원문 블록 순서, 다중 공백, 원형 번호 보기를 최대한 보존하는 별도 레거시 파서를 적용했다. 2015 복원은 210문항으로 재구성했다.
- 2016 복원 DOCX는 PyMuPDF가 XML 조각을 섞어 읽는 문제가 있어 OOXML 본문을 직접 읽어 112문항으로 재제작했다.
- ㄱ/ㄴ/ㄷ 조합형 문항은 ㄱ/ㄴ/ㄷ 진술을 본문에 남기고 조합 보기를 선지로 분리하도록 보정했다.
- 검증 스크립트는 정상적인 단답/주관식 복원 문항을 파싱 실패로 세지 않도록 바꾸고, 임시 답안은 `answer_placeholder`로 따로 표시하게 했다.
- 추가 강의록은 `0427_2_biofeedback-yoga-taichi.html`에 수정본을 반영하고, `0428_6_healthcare-framework-act.html`을 정식 파일명으로 편입해 시간표와 교수 매핑에 연결했다.
